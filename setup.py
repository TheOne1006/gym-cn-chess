from setuptools import setup, find_packages

setup(name='gym_cn_chess',
      version='0.0.1',
      packages=find_packages(),
      install_requires=['numpy', 'pygame'],  # And any other dependencies foo needs
      )
